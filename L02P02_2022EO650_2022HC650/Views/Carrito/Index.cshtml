@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<L02P02_2022EO650_2022HC650.Models.Libro>

<h2>Listado de Libros</h2>

<table class="table">
    <thead>
        <tr>
            <th>Imagen</th>
            <th>Nombre</th>
            <th>Autor</th>
            <th>Categoría</th>
            <th>Precio</th>
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var libro in Model)
        {
            <tr>
                <td>
                    <img src="@libro.UrlImagen" alt="Imagen del libro" width="50" />
                </td>
                <td>@libro.Nombre</td>
                <td>@libro.IdAutor</td>
                <td>@libro.IdCategoria</td>
                <td>$@libro.Precio</td>
                <td>
                    <form asp-action="AgregarAlCarrito" asp-controller="Libros" method="post">
                        <input type="hidden" name="idLibro" value="@libro.Id" />
                        <button type="submit" class="btn btn-primary">Agregar al Carrito</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="alert alert-info">
    <strong>Total de Libros en el Carrito: </strong> @ViewBag.TotalLibros <br>
    <strong>Total a Pagar: </strong> $@ViewBag.TotalPrecio
</div>

<button class="btn btn-success">Completar Compra</button>



